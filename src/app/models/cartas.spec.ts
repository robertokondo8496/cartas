import { Cartas } from './cartas';
import { Naipes } from './naipes';

describe('Cartas', () => {

  let carta: Cartas;

  beforeEach(() => {
    carta = new Cartas(
      'A',
      'Paus',
    )
  })
});
